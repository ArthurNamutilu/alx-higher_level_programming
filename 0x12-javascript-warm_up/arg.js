#!/usr/bin/node

console.log(process.argv[0]);  // path to the Node.js executable
console.log(process.argv[1]);  // path to the script that is being executed
/*
const length = process.argv.length - 2;
if (length === 0) {
  console.log('No argument');
} else if (length === 1) {
  console.log('Argument found');
} else {
  console.log('Arguments found');
}
*/
